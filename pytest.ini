[pytest]
testpaths = tests
addopts = -v --tb=short --ignore=tests/e2e
markers =
    slow: тесты с реальными потоками, ретраями или тяжёлыми моками (запускать отдельно)
    e2e: смоки без моков внешних сервисов (RUN_E2E=1)
